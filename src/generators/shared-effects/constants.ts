/** Shared upper bound for square or shard debris size. */
export const MAX_FRAGMENT_SIZE = 8

/** Shared upper bound for polar shockwave band thickness. */
export const MAX_SHOCKWAVE_THICKNESS = 6

/** Shared default canvas edge used to scale effect distances. */
export const DEFAULT_CANVAS_SIZE = 128
